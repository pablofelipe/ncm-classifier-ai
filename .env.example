# Optional — only needed once the Gemini rerank path is implemented.
# Leave unset to run the shipping pipeline (Passthrough rerank).
# GEMINI_API_KEY=your-gemini-api-key-here
CHROMA_PATH=data/chroma
TIPI_DATA_DIR=data/tipi
CONFIDENCE_THRESHOLD=0.7
